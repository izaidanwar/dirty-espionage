[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python -m pip install -r requirements.txt"]

[start]
cmd = "python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
[providers]
python = {}

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python -m pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
[providers]
python = {}

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["cd backend && python -m pip install -r requirements.txt"]

[start]
cmd = "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["cd backend && python -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt"]

[start]
cmd = "cd backend && . .venv/bin/activate && uvicorn main:app --host 0.0.0.0 --port $PORT"
